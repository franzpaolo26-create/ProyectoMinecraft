# 🖥️ Backend

API REST Node.js + Express + PostgreSQL.

## Setup
```bash
npm install
cp .env.example .env
npm run dev   # http://localhost:3000
```

Ver `tasks/backend/tareas.md` para tareas pendientes.
